# CONNECT-4 (MICCAI 2026) — Python dependencies
# Core deep learning
torch>=2.0
torchvision
einops

# Graph attention / hypergraph fusion (Figure 1B)
torch-geometric>=2.4

# Foundation models
transformers>=4.40           # Clinical ModernBERT (text encoder)
monai>=1.3                   # BrainIAC / BMMAE backbones

# Neuroimaging IO, registration, preprocessing
nibabel>=5.0
nilearn>=0.10
scikit-image>=0.20
scipy>=1.10
scikit-learn>=1.3            # patient-grouped 5-fold downstream evaluation
pyradiomics>=3.0             # subject-specific ROI radiomics extraction

# Normative model (Potvin mmc2.xlsm)
openpyxl>=3.1

# Data / config / utils
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
matplotlib>=3.7              # magma real-vs-synthetic visualisation
tqdm

# --- external tools (install separately, not pip) ---
# SynthSeg  : segmentation — RUN EXTERNALLY by you  https://github.com/BBillot/SynthSeg
# fMRIPrep   : mandatory production BOLD preprocessing       https://fmriprep.org
# FSL        : FLIRT/MCFLIRT used only by the non-production debug backend
# BrainIAC   : place BrainIAC/ + BrainIAC.ckpt at repo root  https://github.com/AIM-KannLab/BrainIAC
# BrainLM    : required serialized 4D module for perceptual loss  https://github.com/vandijklab/BrainLM
# AnatCL     : pretrained ROI feature extraction             https://github.com/EIDOSLAB/AnatCL
# SLIM-Brain : required for FID; a trained logits head is additionally required for IS
